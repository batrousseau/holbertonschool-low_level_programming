#include "main.h"
/* more headers goes there */

/**
* _strlen - return the length of
* a string
* @s : a pointer to a string
* Return:an integer
*/


int _strlen(char *s)
{

int i = 0;

for (i = 0; *(s + i) != 0; i++)
{
}

return (i);

}
