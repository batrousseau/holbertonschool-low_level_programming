#include "main.h"
/* more headers goes there */

/**
* add - Add two integers and return the result
* @a: an integer
* @b: integer are two integers that are being added
* Return: c is the result of the function
*/
int add(int a, int b)
{

	int c = a + b;

	return (c);
}
